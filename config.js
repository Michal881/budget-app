// Optional runtime configuration for the frontend.
//
// Local development usually needs no changes:
// - when opening frontend on localhost, the app automatically uses http://127.0.0.1:8000
//
// Production (frontend + backend on different domains):
// - set apiBaseUrl to your backend URL, e.g. https://your-backend.onrender.com
window.BUDGET_APP_CONFIG = {
  apiBaseUrl: ""
};
